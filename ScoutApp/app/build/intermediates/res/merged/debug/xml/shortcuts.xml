<shortcuts xmlns:android="http://schemas.android.com/apk/res/android">
    <shortcut
        android:shortcutId="Recommended"
        android:icon="@drawable/recommendedshortcut"
        android:enabled="true"
        android:shortcutShortLabel="@string/short_label1"
        android:shortcutLongLabel="@string/long_label1"
        >
        <intent
            android:action="android.intent.action.VIEW"
            android:targetPackage="com.syracuse.rameka.scoutapp"
            android:targetClass="com.syracuse.rameka.scoutapp.Navigation" />
        <!-- If your shortcut is associated with multiple intents, include them
             here. The last intent in the list determines what the user sees when
             they launch this shortcut. -->
        <categories android:name="android.shortcut.conversation" />
    </shortcut>

</shortcuts>